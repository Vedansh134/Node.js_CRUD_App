kind: Service
apiVersion: v1
metadata:
  name: mongodb-svc
  namespace: crudapp-database
spec:
  #clusterIP: None
  selector:
    app: mongo
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP

